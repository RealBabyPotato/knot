[package]
name = "knot-desktop"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
tauri = { version = "1", features = [] }

[build-dependencies]
tauri-build = { version = "1", features = [] }
